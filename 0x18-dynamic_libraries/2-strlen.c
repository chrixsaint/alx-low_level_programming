#include "main.h"
#include <string.h>
/**
 * _strlen - This functions resets a value.
 * @s: This is a param.
 *
 * Return: returns len.
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
