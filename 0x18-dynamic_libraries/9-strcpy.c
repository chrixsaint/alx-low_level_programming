#include "main.h"
#include <string.h>
/**
 * _strcpy - This functions resets a value.
 * @dest: This is a param.
 * @src: This is a param.
 * Return: Returns dest.
 */
char *_strcpy(char *dest, char *src)
{

	strcpy(dest, src);
	return (dest);
}
